const axios = require('axios');
async function test() {
    try {
        const url = 'http://localhost:5001/api/clinic-admin/1'; // Wait, I should test the live url, or local? I'll test local DB with direct functions.
    } catch(e) {}
}
